var React = require('react');

var Hello = React.createClass({
    render: function() {
        return (
            <h1>
                Hello, world!
            </h1>
        );
    }
});

export default Hello;
