import React from 'react';
/*eslint-disable no-unused-vars*/
import Hello from '_app/hello.jsx';
/*eslint-enable no-unused-vars*/

function render() {
    React.render(
        <Hello/>,
        document.body
    );
}

export default render;
